# Supabase Edge Functions — secrets à définir via :
# supabase secrets set GEMINI_API_KEY=...
# (SUPABASE_URL et SUPABASE_SERVICE_ROLE_KEY sont injectés automatiquement)

GEMINI_API_KEY=your_google_gemini_api_key_here

# Instagram Graph API (stocké dans company_settings via l'app)
# Clés par entreprise, pas de variable globale nécessaire
